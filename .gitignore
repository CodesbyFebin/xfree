node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example

# next-app/ is a separate Next.js project (see next-app/README or the root
# README's "Repository layout" section) with its own build pipeline —
# its build output and dependency tree don't belong in this repo's history.
next-app/.next/
next-app/node_modules/
next-app/tsconfig.tsbuildinfo

# Regenerated by `npm run audit:tools` on every run — not source.
reports/tool-audit.json
# api/index.js is checked in — it's the Vercel function bundle produced by
# `npm run build:function` from api/_handler.ts. Vercel's build pipeline
# proved too opinionated to reliably regenerate it during deploy, so we
# commit the artifact and regenerate before every push that touches src/.
